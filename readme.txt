Cash register built in C++ that runs in terminal 
  *Selects item by typing in sku
  *Adds 8% sales tax
  *Optional 10% coupon 

Will compile. Needs SQL database with sku number field, name field, price field. 
Will eventually be places in a raspberry pi and have screen, scanner, and cash drawer
